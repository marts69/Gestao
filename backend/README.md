# Backend

Pasta reservada para a API Express/Prisma na migracao por fases.

No momento, o projeto ainda usa `server.ts` e `routes.ts` na raiz. Esta pasta ja existe para receber a separacao gradual sem quebrar o fluxo atual.