# Frontend

Pasta reservada para a interface React/Vite na migracao por fases.

No momento, o projeto ainda usa a estrutura raiz antiga. Quando a migracao avancar, os arquivos de UI vao para esta pasta sem mudar o comportamento da aplicacao.